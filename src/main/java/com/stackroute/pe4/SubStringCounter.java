package com.stackroute.pe4;

public class SubStringCounter {
    public String stringCounter(String string,String subString){
        if(string.contains(" "))
        if(string.contains(subString))
    }
}
